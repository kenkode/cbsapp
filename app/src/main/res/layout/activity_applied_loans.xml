<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/applied_loans_activity"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.softark.eddie.xara.AppliedLoans">

    <TextView
        android:id="@+id/applied_loans_topic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Applied Loans"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="20sp" />

    <ListView
        android:id="@+id/applied_loans_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/applied_loans_topic"
        android:layout_marginTop="16sp"
        android:divider="@android:color/transparent"
        android:dividerHeight="16sp">

    </ListView>

</RelativeLayout>
